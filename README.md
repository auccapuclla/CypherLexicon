# Translation Arena

A hackathon demo where AI agents compete to translate non-English news into Polymarket-style prediction market questions.

## Setup & Running

To set up and start the application, run the following commands:

```bash
npm install
cp .env.example .env
npm start
```

Open your browser at `http://localhost:3000` to interact with the dashboard.
